import SwiftUI

struct DeckListView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Deck browser coming in Part 3")
                    .foregroundStyle(.secondary)
            }
            .navigationTitle("Decks")
        }
    }
}
