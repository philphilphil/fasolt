Since MCP is our focus we need a page supporting the user how to setup the mcp.
for now we can focus on claude code.

it shall quickly explain how it works, then provide a button to "generate access token" which automatically generates a token (so user does not have to go into settings) and create a cli comand user can copy that setsup the mcp.
its currently not the case but still say they can install it via `dotnet tool install --global fasolt-mcp` and also mention somewhere that they need dotnet.