Since this APp will be (free) SAAS i still need some kind of dashboard and backend to manage users if someone missbehaves.
Add Roles (just Admin and User). add an admin area with user overview and how many decks etc they have.
make sure the admin endpoints are not usable via mcp and normal api!